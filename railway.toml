[build]
builder = "nixpacks"
buildCommand = "npm install && cd Client && npm install && npm run build"

[deploy]
startCommand = "cd Client && ./start.sh"
healthcheckPath = "/"
healthcheckTimeout = "300s"
restartPolicyType = "ON_FAILURE"

[deploy.envs]
PORT = "4173"
NIXPACKS_NODE_VERSION = "18"